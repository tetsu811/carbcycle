# 碳循環熱量計算 — 部署說明

三個步驟：設定 Supabase → 填入金鑰 → 部署到 Vercel。全程免費。

## 1. Supabase 設定（約 5 分鐘）

1. 到 [supabase.com](https://supabase.com) 建一個新專案（或用現有專案也可以，資料表不會衝突）
2. **SQL Editor** → 貼上 `supabase-setup.sql` 全部內容 → Run
3. **Authentication → Users → Add user**：
   - Email：你的 email
   - Password：自訂密碼（這就是手機/電腦登入用的密碼）
   - 勾選 **Auto Confirm User**
4. **Authentication → Sign In / Providers**：把 **Allow new users to sign up 關閉**（重要！關掉後只有你這個帳號能登入）
5. **Project Settings → API** 複製兩個值：
   - Project URL
   - anon public key

## 2. 填入金鑰

打開 `index.html`，最上方 script 區找到：

```js
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```

換成剛剛複製的值。anon key 本來就是公開金鑰，配合 RLS 只有登入者能存取自己的資料，寫在前端是安全的。

## 3. 部署到 Vercel

```bash
cd carbcycle
git init
git add -A
git commit -m "carb cycling tracker"
git branch -M main
# 先到 GitHub 開一個新 repo（例如 carbcycle），然後：
git remote add origin https://github.com/tetsu811/carbcycle.git
git push -u origin main
```

Vercel → **Add New → Project** → Import `carbcycle` repo → Framework Preset 選 **Other** → Deploy。
（純靜態頁面，不需要任何環境變數或 build 設定）

之後改動只要 `git push`，Vercel 會自動重新部署。

## 4. 開始使用

- 手機和電腦都開部署後的網址，用步驟 1-3 設的 email + 密碼登入一次即可，之後會記住
- 手機：Safari/Chrome 開啟 → 分享 → **加入主畫面**，就像 App 一樣
- 任一裝置記錄後約 1 秒自動上傳；切回 App 時會自動抓最新資料
- 上方會顯示同步狀態（已同步 / 同步中 / 離線）

## 舊資料搬移

之前那個單機版 HTML 如果有記錄資料：舊版「設定 → 匯出備份」下載 JSON → 新網站「設定 → 匯入備份」，會自動同步上雲。

## 常見問題

- **登入顯示錯誤**：確認 Supabase 使用者有勾 Auto Confirm；或到 Users 頁面確認帳號存在
- **顯示「未設定 Supabase，離線模式」**：index.html 頂部金鑰沒填或格式不對
- **同步失敗**：通常是斷網，App 會自動重試；資料先存本機不會不見
